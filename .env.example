# Copy to .env and fill in. .env is gitignored.
# Any OpenAI-compatible endpoint works — see eval/README.md for provider URLs.

HERMES_API_KEY=
HERMES_BASE_URL=https://inference-api.nousresearch.com/v1
HERMES_MODEL=Hermes-4.3-36B
